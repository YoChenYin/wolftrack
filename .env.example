DATABASE_URL="postgresql://user@localhost:5432/wolftrack?schema=public"

# https://polygon.io/dashboard/api-keys
POLYGON_API_KEY=

# 排程觸發密鑰：/api/cron/* 這兩支 endpoint 要求 Authorization: Bearer <CRON_SECRET>
# 自己隨便產生一組長字串即可，例如：openssl rand -hex 32
CRON_SECRET=
